/**
 * AddColumn — disabled in Phase 2.
 * Columns are fixed and non-configurable per PRD section 3.1.
 * This component is retained as a placeholder and will be removed in TICKET-P2-004.
 */
export function AddColumn() {
  return null;
}
